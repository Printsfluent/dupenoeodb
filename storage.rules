rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    function signedIn() {
      return request.auth != null;
    }

    match /workspaces/{workspaceId}/bases/{baseId}/attachments/{attachmentId} {
      allow read, write: if signedIn();
    }
  }
}
